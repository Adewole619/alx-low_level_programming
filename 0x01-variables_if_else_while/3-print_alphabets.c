#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/* more headers goes there */
/**
 * main - pint alphabet in lower case and
 * then in  upper case
 * Return: Always 0.
 */

int main(void)

{
char letter

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(letter);

	for (letter = 'A'; letter <= 'Z'; letter++)
		putchar(ltter);

	putchar('\n')

	return (0);

}
